
// #include <iostream>
// using namespace std;

// int main(){
// /*
// Write a program that asks "male or female? (m or f)" and stores it in a char variable, asks for age in years, weight in kg, and height in cm.

// The program will first calculate the BMR of the person and then calculate the # of cookies eaten by using # of cookies eaten = BMR / calories in Cookie.
// Amount of calories in a cookie = 230.0
// BMR equation varies.  You have to calculate the BMR depending on the person.

// if the gender is m and the age is less than 65, Calculate the BMR as such:
// BMR = 88.362 + (13.397 * weight) + (4.799 * height) - (5.677 * age);
// if the gender is m and the age is greater or equal to 65,  or the gender is f, Calculate the BMR as such:
// BMR = 447.593 + (9.247 * weight) + (3.098 * height) - (4.330 * age);
// */

// string gender;
// int age;
// int weight;
// int height;
//   cout << "Enter gender: ";
//     cin >> gender;
//   cout << "Age: ";
//     cin >> age;
//   cout << "weight in kg :";
//     cin >> weight;
//   cout << "height in cm: "; 
//     cin >> height;
//  int BMR;
//  int cookies;

// if (gender == "male" && age <= 65) {
//   BMR = 88.362 + (13.397 * weight) + (4.799 * height) - (5.677 * age);
//     cookies = BMR / 230.0;

//   cout << "Your BMR is " << BMR << "." << endl; 
//   cout << "And " << cookies << " are cookies eaten." << endl;

// }
//  else if (age >= 65 || gender == "female") {
//    BMR = 447.593 + (9.247 * weight) + (3.098 * height) - (4.330 * age);
//     cookies = BMR / 230.0;

//    cout << "Your BMR is " << BMR << "." << endl;
//    cout << "And " << cookies << " are cookies eaten." << endl;
//  }



// }